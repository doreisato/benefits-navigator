- 2026-03-08 (Loop): Replaced number input with text input to allow comma-separated currency values for Income. Removed emojis from benefits data to strictly follow the monochrome design system.
- 2026-03-08 (Loop): Added "How many children under 5?" field that conditionally appears when user selects "I have children under 18". Fixes WIC eligibility accuracy by eliminating false positives for families with only older children. Field includes helper text explaining it's used for WIC (Women, Infants & Children) program eligibility.
- 2026-03-08 (Loop): Added explicit form validation guardrails on the calculator: ZIP is sanitized to digits and constrained to 5 characters, income must be greater than $0, inline error guidance is shown for both fields, and the submit button stays disabled until required inputs are valid.
- 2026-03-08 (Loop): Implemented the missing homepage calculator flow end-to-end with prefilled realistic defaults (60601, household 3, $2000, children), wired it to the existing benefits engine, surfaced likely-eligible programs with direct apply links and monthly estimate totals, tightened category pills to monochrome rounded-md styling, and updated footer link to infinitemachines.ai.
